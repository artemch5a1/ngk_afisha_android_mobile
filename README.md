# 📱 НГК Афиша — мобильное приложение

<div align="center">

**Android-приложение для просмотра событий и взаимодействия с приглашениями внутри колледжа НГК**

[![Kotlin](https://img.shields.io/badge/Kotlin-2.0.21-blue.svg)](https://kotlinlang.org/)
[![Android](https://img.shields.io/badge/Android-API%2030+-green.svg)](https://developer.android.com/)
[![Compose](https://img.shields.io/badge/Jetpack%20Compose-2024.09.00-orange.svg)](https://developer.android.com/jetpack/compose)
[![Hilt](https://img.shields.io/badge/Hilt-2.52-purple.svg)](https://dagger.dev/hilt/)

</div>

---

## 📖 Описание

**НГК Афиша** — мобильное приложение для студентов и сотрудников Нижегородского государственного колледжа. Приложение позволяет узнавать о предстоящих мероприятиях колледжа, получать приглашения на события и управлять своей активностью.

Приложение работает совместно с backend-сервисами НГК Афиша и предоставляет удобный интерфейс для взаимодействия с системой событий колледжа.

---

## ✨ Основные возможности

### 👤 Для пользователей

- 🔐 **Регистрация и авторизация** с сохранением сессии
- 📅 **Просмотр всех доступных событий** колледжа
- 🔍 **Детальная информация** о каждом событии
- 📨 **Получение приглашений** на мероприятия
- ✅ **Управление откликами** (принять/отклонить приглашение)
- 👤 **Профиль пользователя** с возможностью редактирования
- 🎨 **Настройка внешнего вида** (светлая/темная/системная тема)
- 🔄 **Автоматическое восстановление сессии** при перезапуске

### 📝 Для организаторов (публикаторов)

- ➕ **Создание и редактирование событий**
- 👥 **Управление участниками** мероприятий
- 📤 **Отправка приглашений** студентам
- 📊 **Управление статусами приглашений**
- ✏️ **Редактирование профиля организатора**

---

## 🏗 Архитектура

Проект построен на принципах **Clean Architecture** с разделением на модули:

```
📦 ngk_afisha_android_mobile/
├── 📱 app/                    # Android-модуль (UI, DI, настройки)
│   ├── presentation/          # Экран, композиции, ViewModels
│   │   ├── screens/          # Все экраны приложения
│   │   ├── components/       # Переиспользуемые UI-компоненты
│   │   ├── navigation/       # Навигация (NavHost, BottomBar)
│   │   ├── viewmodels/       # ViewModels для экранов
│   │   └── ui/theme/         # Тема приложения (цвета, типографика)
│   ├── di/                   # Модули зависимостей (Hilt)
│   └── settings/             # Локальное хранилище (DataStore)
│
├── 🎯 domain/                 # Бизнес-логика (чистый JVM)
│   ├── models/               # Доменные модели
│   ├── abstractions/         # Интерфейсы репозиториев и сервисов
│   └── contracts/            # Контракты для API
│
├── 💾 data/                   # Слой данных (чистый JVM)
│   ├── repositories/         # Реализации репозиториев
│   ├── remote/               # API-клиенты (Retrofit)
│   ├── mappers/              # Маппинг DTO → Domain
│   └── common/               # Общие утилиты (HTTP, auth)
│
└── 🔧 application/           # Use Cases (чистый JVM)
    └── useCases/             # Бизнес-логика приложения
```

### Паттерны и подходы

- **MVVM** — Model-View-ViewModel для UI
- **Repository Pattern** — абстракция над источниками данных
- **Use Cases** — инкапсуляция бизнес-логики
- **Dependency Injection** — Hilt для управления зависимостями
- **Reactive Programming** — Kotlin Flow для асинхронных операций

---

## 🛠 Технологический стек

### Основные технологии

| Категория | Технологии |
|-----------|------------|
| **Язык** | Kotlin 2.0.21 |
| **UI Framework** | Jetpack Compose (Material 3) |
| **Архитектура** | Clean Architecture, MVVM |
| **DI** | Hilt (Dagger) |
| **Асинхронность** | Kotlin Coroutines, Flow |
| **Сеть** | Retrofit 2, OkHttp 3 |
| **Сериализация** | Gson |
| **Изображения** | Coil |
| **Локальное хранилище** | DataStore Preferences |
| **Навигация** | Jetpack Navigation Compose |

### Версии и требования

- **Min SDK:** 30 (Android 11)
- **Target SDK:** 36 (Android 15)
- **Compile SDK:** 36
- **Java:** 11
- **Gradle:** 8.13.0
- **AGP:** 8.13.0

---

## 🌐 API и Backend

Приложение взаимодействует с следующими сервисами:

| Сервис | Назначение | Base URL |
|--------|------------|----------|
| **Identity Service** | Регистрация, авторизация, JWT-токены, управление аккаунтами | `https://identity.ngkapi.ru/` |
| **Event Service** | События, приглашения, участники, роли | `https://events.ngkapi.ru/` |
| **MinIO** | Загрузка изображений (presigned URLs) | - |

### Аутентификация

- Используется **JWT (JSON Web Tokens)** для авторизации
- Токен автоматически добавляется в заголовки запросов через `AuthInterceptor`
- Сессия сохраняется локально и восстанавливается при перезапуске приложения

---

## 🚀 Быстрый старт

### Предварительные требования

- **Android Studio** Hedgehog (2023.1.1) или новее
- **JDK 11** или выше
- **Android SDK** с API 30+
- **Git**

### Установка

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/artemch5a1/ngk_afisha_android_mobile.git
   cd ngk_afisha_android_mobile/develop
   ```

2. **Откройте проект в Android Studio:**
   - File → Open → выберите папку `develop`
   - Дождитесь синхронизации Gradle

3. **Настройте API endpoints (опционально):**
   
   Если нужно изменить адреса API, отредактируйте файлы:
   - `data/src/main/java/com/example/data/identityService/common/remote/IdentityApiService.kt`
   - `data/src/main/java/com/example/data/eventService/remote/EventApiService.kt`
   
   ```kotlin
   private const val BASE_URL = "https://your-api-url.com/"
   ```

4. **Запустите приложение:**
   - Подключите Android-устройство или запустите эмулятор
   - Нажмите Run (▶️) или `Shift + F10`

---

## 📂 Структура модулей

### `:app` — Android-модуль

Основной модуль приложения, содержащий UI и Android-специфичный код.

**Ключевые компоненты:**
- `presentation/screens/` — все экраны приложения
- `presentation/viewmodels/` — ViewModels с бизнес-логикой UI
- `presentation/components/` — переиспользуемые Compose-компоненты
- `presentation/navigation/` — навигационный граф
- `presentation/ui/theme/` — тема приложения (цвета, типографика)
- `di/` — модули Hilt для dependency injection
- `settings/` — локальное хранилище (DataStore)

### `:domain` — Доменный слой

Чистый JVM-модуль с бизнес-логикой, не зависящий от Android.

**Содержит:**
- Доменные модели (`models/`)
- Интерфейсы репозиториев и сервисов (`abstractions/`)
- Контракты для API (`contracts/`)
- Исключения и ошибки (`exceptions/`)

### `:data` — Слой данных

JVM-модуль для работы с внешними источниками данных.

**Содержит:**
- Реализации репозиториев (`repositories/`)
- API-клиенты Retrofit (`remote/`)
- DTO-модели (`dto/`)
- Мапперы DTO → Domain (`mappers/`)
- HTTP-клиент и interceptors (`common/`)

### `:application` — Слой Use Cases

JVM-модуль с бизнес-логикой приложения.

**Содержит:**
- Use Cases для различных операций (`useCases/`)
- Базовый класс `BaseUseCase` с обработкой ошибок
- Сервисы (например, `Session` для управления сессией)

---

## 🎨 Особенности UI/UX

### Темы оформления

Приложение поддерживает три режима темы:
- 🌞 **Светлая тема** — классическое светлое оформление
- 🌙 **Темная тема** — комфортное оформление для темного времени суток
- 🔄 **Системная тема** — автоматическое переключение по настройкам устройства

Настройки темы сохраняются локально и применяются при каждом запуске.

### Скриншоты экранов

Ниже представлены основные экраны приложения в **светлой** и **темной** темах.

#### Главный экран

<div align="center">
<img src="Главная (светлая тема).png" width="45%" alt="Главный экран — светлая тема" />
<img src="Главный_экран(темная тема).png" width="45%" alt="Главный экран — тёмная тема" />
</div>

#### Экран заявок

<div align="center">
<img src="Заявки(светлая тема).png" width="45%" alt="Заявки — светлая тема" />
<img src="Заявки(темная тема).png" width="45%" alt="Заявки — тёмная тема" />
</div>

#### Экран приглашений

<div align="center">
<img src="Приглашения(светлая тема).png" width="45%" alt="Приглашения — светлая тема" />
<img src="Приглашения(темная тема).png" width="45%" alt="Приглашения — тёмная тема" />
</div>

#### Подробности события

<div align="center">
<img src="Подробности_события(светлая тема).png" width="45%" alt="Подробности события — светлая тема" />
<img src="Подробности_события(темная тема).png" width="45%" alt="Подробности события — тёмная тема" />
</div>

#### Профиль пользователя

<div align="center">
<img src="Профиль(светлая тема).png" width="45%" alt="Профиль пользователя — светлая тема" />
<img src="Профиль (темная тема).png" width="45%" alt="Профиль пользователя — тёмная тема" />
</div>

### Навигация

- **Bottom Navigation Bar** — быстрый доступ к основным разделам
- **Навигационный граф** — централизованное управление переходами
- **Deep Linking** — поддержка прямых ссылок на события и приглашения

---

## 🔐 Безопасность и хранение данных

### Локальное хранилище

Приложение использует **DataStore Preferences** для хранения:
- Токен доступа (JWT)
- Email и пароль (для автозаполнения)
- Настройки темы

**Важно:** В текущей версии пароль хранится в открытом виде. Для production рекомендуется использовать **EncryptedDataStore** или **EncryptedSharedPreferences**.

### Сессия

- Сессия автоматически восстанавливается при перезапуске приложения
- При истечении токена пользователь автоматически перенаправляется на экран входа
- Сохраненные учетные данные используются для автозаполнения полей входа

---

## 🧪 Тестирование

### Запуск тестов

```bash
# Unit-тесты
./gradlew test

# Instrumented-тесты
./gradlew connectedAndroidTest
```

### Структура тестов

- `app/src/test/` — unit-тесты
- `app/src/androidTest/` — instrumented-тесты

---

## 📦 Сборка

### Debug-сборка

```bash
./gradlew assembleDebug
```

APK будет находиться в `app/build/outputs/apk/debug/`

### Release-сборка

```bash
./gradlew assembleRelease
```

**Примечание:** Для release-сборки необходимо настроить signing config в `app/build.gradle.kts`

---

## 🔗 Связанные проекты

- **Backend репозиторий:** [ngk_afisha_backend](https://github.com/artemch5a1/ngk_afisha_backend)
- **Дизайн в Figma:** [NGKAfisha Design](https://www.figma.com/design/4MBvuGuI88IimuwWQ06PeA/NGKAfisha)

---

## 👥 Разработка

### Вклад в проект

1. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
2. Внесите изменения и закоммитьте (`git commit -m 'Add amazing feature'`)
3. Запушьте в ветку (`git push origin feature/amazing-feature`)
4. Создайте Pull Request

### Статус разработки

Проект находится в активной разработке. В планах:
- Улучшение безопасности хранения данных
- Оптимизация производительности
- Расширение функциональности для организаторов
- Добавление уведомлений о новых событиях

---
